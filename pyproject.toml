[project]
name = "fastapi-mcp-string-manip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi-mcp>=0.4.0",
    "fastapi[standard]>=0.128.6",
]
